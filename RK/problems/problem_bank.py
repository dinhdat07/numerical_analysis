from sympy import symbols, sin, cos, exp, sqrt, ln, simplify
t, y = symbols('t y')

# label, f_expr, x0, y0, h, x_end, y_exact_expr
problems_info = [
    # 1.a to 1.d
    ("1.a", "t*exp(3*t) - 2*y", 0, 0, 0.1, 1, "(1/5)*t*exp(3*t) - (1/25)*exp(3*t) + (1/25)*exp(-2*t)"),
    ("1.b", "1 + (t - y)**2", 2, 1, 0.1, 3, "t + 1/(1 - t)"),
    ("1.c", "1 + y/t", 1, 2, 0.1, 2, "t*ln(t) + 2*t"),
    ("1.d", "cos(2*t) + sin(3*t)", 0, 1, 0.1, 1, "(1/2)*sin(2*t) - (1/3)*cos(3*t) + 4/3"),

    # 2.a to 2.d
    ("2.a", "exp(t-y)", 0, 1, 0.1, 1, "ln(exp(t) + exp(1) - 1)"),
    ("2.b", "(1 + t)/(1 + y)", 1, 2, 0.1, 2, "sqrt(t**2 + 2*t + 6) - 1"),
    ("2.c", "-y + t*sqrt(y)", 2, 2, 0.1, 3, "(t - 2 + sqrt(2)*exp(1-t/2))**2"),
    ("2.d", "t**(-2)*(sin(2*t) - 2*t*y)", 1, 2, 0.1, 2, "0.5*t**(-2)*(4 + cos(2) - cos(2*t))"),

    # 3.a to 3.d
    ("3.a", "y/t - (y/t)**2", 1, 1, 0.1, 2, "t/(1 + ln(t))"),
    ("3.b", "1 + y/t + (y/t)**2", 1, 0, 0.1, 3, "t*tan(ln(t))"),
    ("3.c", "-(y + 1)*(y + 3)", 0, -2, 0.1, 2, "-3 + 2/(1 + exp(-2*t))"),
    ("3.d", "-5*y + 5*t**2 + 2*t", 0, 1/3, 0.1, 1, "t**2 + (1/3)*exp(-5*t)"),

    # 4.a to 4.d
    ("4.a", "(2 - 2*t*y)/(t**2 + 1)", 0, 1, 0.1, 1, "(2*t + 1)/(t**2 + 1)"),
    ("4.b", "y**2/(1 + t)", 1, -1/(ln(2)), 0.1, 2, "-1/ln(1 + t)"),
    ("4.c", "(y**2 + y)/t", 1, -2, 0.1, 3, "2*t/(1 - 2*t)"),
    ("4.d", "-t*y + 4*t/y", 0, 1, 0.1, 1, "sqrt(4 - 3*exp(-(t**2)))")
]

